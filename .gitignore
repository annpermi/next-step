node_modules/
dist/
.env
.env.local
*.log
npm-debug.log*
.DS_Store
claude.md

# roomwalk pipeline scratch: source segments, Swift binaries, intermediate
# frames. The served output lives in public/roomwalk/ and is committed.
assets/roomwalk/
